import { AccountInterface } from 'starknet';
import { Propose, Vote } from './types';
export declare class StarkNetSig {
    readonly address: string;
    constructor(address: string);
    sign(signer: AccountInterface, address: string, message: any, types: any, primaryType: any): Promise<{
        address: string;
        sig: import("@noble/curves/abstract/weierstrass").SignatureType;
        data: any;
    }>;
    send(envelop: any): Promise<any>;
    propose(web3: AccountInterface, address: string, message: Propose): Promise<{
        address: string;
        sig: import("@noble/curves/abstract/weierstrass").SignatureType;
        data: any;
    }>;
    vote(web3: AccountInterface, address: string, message: Vote): Promise<{
        address: string;
        sig: import("@noble/curves/abstract/weierstrass").SignatureType;
        data: any;
    }>;
}
