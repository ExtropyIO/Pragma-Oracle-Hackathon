import { Account } from 'starknet';
import type { EthSigProposeMessage, EthSigVoteMessage, VanillaVoteMessage, VanillaProposeMessage, Envelope, ClientOpts, ClientConfig, StrategiesAddresses } from '../../../types';
export declare class StarkNetTx {
    config: ClientConfig;
    constructor(opts: ClientOpts);
    getProposeCalldata(strategiesAddresses: StrategiesAddresses, envelope: Envelope<VanillaProposeMessage | EthSigProposeMessage>): Promise<string[]>;
    getVoteCalldata(strategiesAddresses: StrategiesAddresses, envelope: Envelope<VanillaVoteMessage | EthSigVoteMessage>): Promise<string[]>;
    propose(account: Account, envelope: Envelope<VanillaProposeMessage | EthSigProposeMessage>): Promise<import("starknet").InvokeFunctionResponse>;
    vote(account: Account, envelope: Envelope<VanillaVoteMessage | EthSigVoteMessage>): Promise<import("starknet").InvokeFunctionResponse>;
}
