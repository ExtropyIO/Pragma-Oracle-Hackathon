"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const encoding_1 = require("../utils/encoding");
const split_uint256_1 = require("../utils/split-uint256");
function createEthRelayerExecutor(params) {
    const { destination, chainId } = params;
    return {
        type: 'ethRelayer',
        getExecutionData(executorAddress, transactions) {
            const { executionHash } = (0, encoding_1.createExecutionHash)(transactions, destination, chainId);
            const executionHashSplit = split_uint256_1.SplitUint256.fromHex(executionHash);
            return {
                executor: executorAddress,
                executionParams: [destination, executionHashSplit.low, executionHashSplit.high]
            };
        }
    };
}
exports.default = createEthRelayerExecutor;
