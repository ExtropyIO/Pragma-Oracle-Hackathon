export default class Model {
    private tableName;
    private values;
    private valuesImplicitlySet;
    private exists;
    constructor(tableName: string);
    private _update;
    private _insert;
    private _delete;
    setExists(): void;
    initialSet(key: string, value: any): void;
    get(key: string): any;
    set(key: string, value: any): void;
    static loadEntity(tableName: string, id: string): Promise<Record<string, any> | null>;
    save(): Promise<number | number[]>;
    delete(): Promise<void>;
}
