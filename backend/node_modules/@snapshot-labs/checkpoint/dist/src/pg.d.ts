import { Pool } from 'pg';
/**
 * Attempts to connect to the database by the connection string.
 *
 * This returns a pg pool connection object.
 */
export declare const createPgPool: (connectionString: string) => Pool;
