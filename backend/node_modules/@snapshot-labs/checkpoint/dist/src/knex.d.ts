import { Knex } from 'knex';
export type KnexType = {
    name: 'integer' | 'bigint' | 'boolean' | 'text' | 'json';
} | {
    name: 'decimal';
    options: [number, number];
} | {
    name: 'float';
    options: [number];
} | {
    name: 'string';
    options: [number];
};
export declare function getConnectionData(connectionString: string): {
    client: any;
    connection: any;
};
export declare function createKnexConfig(connectionString: string): Knex.Config;
export declare function createKnex(config: string | Knex.Config): Knex<any, unknown[]>;
