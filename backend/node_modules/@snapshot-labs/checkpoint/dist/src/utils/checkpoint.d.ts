import { CheckpointConfig } from '../types';
export declare const getContractsFromConfig: (config: CheckpointConfig) => string[];
export declare const getConfigChecksum: (config: CheckpointConfig) => string;
