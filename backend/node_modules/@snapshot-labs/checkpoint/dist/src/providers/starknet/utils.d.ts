import type { Abi } from 'starknet';
type StructAbi = {
    name: string;
    size: number;
    members: {
        name: string;
        type: string;
    }[];
};
export declare const parseStruct: (type: string, data: string[], { current, structs }: {
    current: number;
    structs: Record<string, StructAbi>;
}) => {};
export declare const parseEvent: (abi: Abi, name: string, data: string[]) => Record<string, any>;
export {};
