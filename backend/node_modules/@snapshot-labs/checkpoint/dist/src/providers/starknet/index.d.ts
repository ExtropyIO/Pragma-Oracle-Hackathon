import { BaseProvider } from '../base';
export declare class StarknetProvider extends BaseProvider {
    private readonly provider;
    private processedPoolTransactions;
    constructor({ instance, log, abis }: ConstructorParameters<typeof BaseProvider>[0]);
    getNetworkIdentifier(): Promise<string>;
    processBlock(blockNum: number): Promise<number>;
    processPool(blockNumber: number): Promise<void>;
    private handleBlock;
    private handlePool;
    private handleTx;
    private getEvents;
}
